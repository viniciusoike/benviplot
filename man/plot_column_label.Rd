% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_column_label.R
\name{plot_column_label}
\alias{plot_column_label}
\title{Column plot with text labels (Deprecated)}
\usage{
plot_column_label(
  data,
  x,
  y,
  label,
  fill,
  variable,
  zero = TRUE,
  flip = TRUE,
  fill_guide = "none",
  palette = "qual_9",
  text_color = "white",
  text_family = "sans",
  text_size = 4,
  scale_name = "",
  scale_label = waiver()
)
}
\arguments{
\item{data}{A data.frame type object}

\item{x}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Variable to be mapped in the x-axis.}

\item{y}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Variable to be mapped in the y-axis.}

\item{label}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Text label to mapped onto the column. Defaults to y variable.}

\item{fill}{Color for the columns. Should only be used if \code{variable}
is missing.}

\item{variable}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Variable to be used as grouping for the color groups. Should
only be used if \code{fill} is missing.}

\item{zero}{Logical indicating whether a horizontal line crossing the y = 0
axis should be plotted.}

\item{flip}{Logical indicating if plot should be flipped}

\item{fill_guide}{Optional indicating if fill guide should be suppressed.}

\item{palette}{String indicating the name of which palette to use.}

\item{text_color}{Color of the text label. Default is \code{"gray20"}.}

\item{text_family}{Font of the text label. Default is \code{"Poppins"}.}

\item{text_size}{Size of the text label. Default is \code{3}.}

\item{scale_name}{String indicating fill legend title.}

\item{scale_label}{String indicating fill legend labels.}
}
\value{
A ggplot2 plot
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}

\code{plot_column_label()} has been deprecated in favor of enhanced \code{plot_column()}.
Use \code{plot_column(text = TRUE, text_inside = TRUE, flip = TRUE)} instead.
}
\details{
\subsection{Migration Guide}{

The functionality of \code{plot_column_label()} is now available in \code{plot_column()}
with the \code{text_inside} parameter:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Old (deprecated):
plot_column_label(df, x = category, y = value, flip = TRUE)

# New (recommended):
plot_column(df, x = category, y = value,
            text = TRUE, text_inside = TRUE, flip = TRUE)
}\if{html}{\out{</div>}}
}
}
\examples{
# Use plot_column() instead
df <- data.frame(
  cat = c("A", "B", "C"),
  value = c(11257, 9874, 8991)
)
plot_column(df, x = cat, y = value, text = TRUE, text_inside = TRUE, flip = TRUE)
}
\keyword{internal}
