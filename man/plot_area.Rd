% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_area.R
\encoding{UTF-8}
\name{plot_area}
\alias{plot_area}
\title{Plot an area chart}
\usage{
plot_area(
  data,
  x,
  y,
  fill = NULL,
  zero = TRUE,
  order = TRUE,
  pal_name = "qual_benvi",
  scale_name = "",
  scale_label = ggplot2::waiver(),
  position = "stack"
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Variable mapped to the x-axis.}

\item{y}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Variable mapped to the y-axis.}

\item{fill}{Fill color for the area. Either a color string (e.g., \code{"blue"},
\code{"#021841"}) for a single static color, or a bare column name (without
quotes) to map a grouping variable to fill color.}

\item{zero}{Whether to draw a horizontal line at \code{y = 0}.}

\item{order}{Whether to order stacked areas by size. The default, \code{TRUE},
places the largest groups on top.}

\item{pal_name}{Name of the palette.}

\item{scale_name}{Fill legend title.}

\item{scale_label}{Fill legend labels.}

\item{position}{Position adjustment passed to \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_area()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot an area chart
}
\examples{
\dontshow{.op <- options(theme_benvi.font_family = "sans")}
# Simple area chart
sao_paulo <- subset(iqa, name_muni == "São Paulo")
plot_area(data = sao_paulo, x = date, y = index)

# Stacked area chart with fill mapping
total <- subset(iqaiw, rooms == "Total")
plot_area(data = total, x = date, y = index, fill = name_muni)
\dontshow{options(.op)}
}
