% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_histogram.R
\name{plot_histogram}
\alias{plot_histogram}
\title{Plot a histogram chart}
\usage{
plot_histogram(
  data,
  x,
  color = "#FFFFFF",
  fill = NULL,
  pal_name = "qual_benvi",
  scale_name = "",
  zero = TRUE,
  bins = NULL,
  method = "fd",
  density = FALSE,
  facet = FALSE,
  ...
)
}
\arguments{
\item{data}{A data.frame type object}

\item{x}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Indicates the numeric variable to be mapped}

\item{color}{Color of the column border. Defaults to \code{"#FFFFFF"} (white).}

\item{fill}{Fill color for the columns. Either a color string (e.g., \code{"blue"},
\code{"#021841"}) for a single static color, or a bare column name (without
quotes) to map a grouping variable to fill color.}

\item{pal_name}{String indicating the name of which palette to use when
\code{fill} is a variable mapping.}

\item{scale_name}{String indicating fill legend title.}

\item{zero}{Logical indicating if a horizontal (y = 0) line should be drawn
on the plot.}

\item{bins}{Number of bins. When specified, overrides \code{method}.}

\item{method}{Character specifying the binning algorithm. Must be one of:
\code{"fd"} (default), \code{"FD"}, \code{"Scott"}, \code{"Sturges"}, \code{"Rice"}, or \code{"sqrt"}.
See Details for algorithm descriptions. Ignored when \code{bins} is specified.}

\item{density}{Logical indicating if density should be plotted on y-axis.}

\item{facet}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Optional variable to facet the graphics.}

\item{...}{Additional parameters to \code{facet_wrap()}}
}
\value{
A ggplot2 object
}
\description{
Plot a histogram chart
}
\details{
\subsection{Binning Methods}{

The \code{method} parameter controls which algorithm is used to compute the optimal
bin width. Available methods:

\describe{
\item{\code{"fd"} or \code{"FD"}}{\strong{Freedman-Diaconis rule} (default). Robust to
outliers, uses IQR. Formula: \eqn{2 * IQR / n^{1/3}}. Best for most
distributions.}
\item{\code{"Scott"}}{\strong{Scott's rule}. Uses standard deviation. Formula:
\eqn{3 * sd / n^{1/3}}. Works well for normal-like distributions.}
\item{\code{"Sturges"}}{\strong{Sturges' formula}. Simple logarithmic rule. Formula:
\eqn{k = \lceil log_2(n) \rceil} bins. Good for roughly normal data.}
\item{\code{"Rice"}}{\strong{Rice rule}. Cube root based. Formula:
\eqn{k = \lceil 2n^{1/3} \rceil} bins. General purpose rule.}
\item{\code{"sqrt"}}{\strong{Square root rule}. Formula: \eqn{k = \lceil \sqrt{n} \rceil}
bins. Simple, tends to oversmooth.}
}

When in doubt, use the default \code{"fd"} (Freedman-Diaconis), which is robust
and works well across different distributions.
}
}
\examples{
\dontshow{.op <- options(theme_benvi.font_family = "sans")}
set.seed(5)
tbl <- data.frame(x = rnorm(n = 1000))

# Default parameters use Freedman-Diaconis
plot_histogram(data = tbl, x = x)
# Use bins to manually choose number of bins
plot_histogram(data = tbl, x = x, bins = 50)
# Example of alternative methods: square root and Rice
plot_histogram(data = tbl, x = x, method = "sqrt")
plot_histogram(data = tbl, x = x, method = "Rice")

# Facet by rooms category
spo <- subset(iqaiw, name_muni == "S\u00e3o Paulo" & rooms != "Total")
plot_histogram(data = spo, x = index, facet = rooms)
\dontshow{options(.op)}
}
