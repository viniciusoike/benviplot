% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_histogram.R
\encoding{UTF-8}
\name{plot_histogram}
\alias{plot_histogram}
\title{Plot a histogram chart}
\usage{
plot_histogram(
  data,
  x,
  color = "#FFFFFF",
  fill = NULL,
  pal_name = "qual_benvi",
  scale_name = "",
  zero = TRUE,
  bins = NULL,
  method = "fd",
  density = FALSE,
  facet = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Numeric variable mapped to the
x-axis.}

\item{color}{Color of the column border. Defaults to \code{"#FFFFFF"} (white).}

\item{fill}{Fill color for the columns. Either a color string (e.g., \code{"blue"},
\code{"#021841"}) for a single static color, or a bare column name (without
quotes) to map a grouping variable to fill color.}

\item{pal_name}{Name of the palette used when \code{fill} maps a variable.}

\item{scale_name}{Fill legend title.}

\item{zero}{Whether to draw a horizontal line at \code{y = 0}.}

\item{bins}{Number of bins. When specified, overrides \code{method}.}

\item{method}{Binning method. Choose \code{"fd"} (the default), \code{"FD"}, \code{"Scott"},
\code{"Sturges"}, \code{"Rice"}, or \code{"sqrt"}. See Details. Ignored when \code{bins} is
specified.}

\item{density}{Whether to map density to the y-axis.}

\item{facet}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Optional variable to facet
the plot. \code{NULL} (the default) draws a single panel.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot a histogram chart
}
\details{
\subsection{Binning methods}{

The \code{method} parameter controls how the function computes the bin width.

\describe{
\item{\code{"fd"} or \code{"FD"}}{Freedman-Diaconis rule. Uses the interquartile
range: \eqn{2 * IQR / n^{1/3}}.}
\item{\code{"Scott"}}{Scott's rule. The formula uses the standard deviation,
\eqn{3 * sd / n^{1/3}}.}
\item{\code{"Sturges"}}{Sturges' formula. Uses
\eqn{k = \lceil log_2(n) \rceil} bins.}
\item{\code{"Rice"}}{Rice rule. Uses
\eqn{k = \lceil 2n^{1/3} \rceil} bins.}
\item{\code{"sqrt"}}{Square-root rule. Uses
\eqn{k = \lceil \sqrt{n} \rceil} bins.}
}
}
}
\examples{
\dontshow{.op <- options(theme_benvi.font_family = "sans")}
# Default parameters use Freedman-Diaconis
plot_histogram(data = mtcars, x = mpg)
# Use bins to manually choose number of bins
plot_histogram(data = mtcars, x = mpg, bins = 10)
# Example of alternative methods: square root and Rice
plot_histogram(data = mtcars, x = mpg, method = "sqrt")
plot_histogram(data = mtcars, x = mpg, method = "Rice")

# Facet by rooms category
spo <- subset(iqaiw, name_muni == "São Paulo" & rooms != "Total")
plot_histogram(data = spo, x = index, facet = rooms)
\dontshow{options(.op)}
}
