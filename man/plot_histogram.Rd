% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_histogram.R
\name{plot_histogram}
\alias{plot_histogram}
\title{Plot a histogram chart}
\usage{
plot_histogram(
  data,
  x,
  color = "#FFFFFF",
  fill = "#3957BD",
  zero = TRUE,
  bins = NULL,
  method = "fd",
  density = FALSE,
  facet = FALSE,
  ...
)
}
\arguments{
\item{data}{A data.frame type object}

\item{x}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Indicates the numeric variable to be mapped}

\item{color}{Color of the line of column.}

\item{fill}{Color of the inner part of the column.}

\item{zero}{Logical indicating if a horizontal (y = 0) line should be drawn
on the plot.}

\item{bins}{Number of bins.}

\item{method}{Character indicating an algorithm to compute optimal number of
bins. See details. Overriden by bins. Defaults to \code{method = "fd"}.}

\item{density}{Logical indicating if density should be plotted on y-axis.}

\item{facet}{<\code{\link[ggplot2:aes_eval]{data-masked}}> Optional variable to facet the graphics.}

\item{...}{Additional parameters to \code{facet_wrap()}}
}
\value{
A ggplot2 object
}
\description{
Plot a histogram chart
}
\examples{
set.seed(5)
tbl <- data.frame(x = rnorm(n = 1000))

# Default parameters use Freedman-Diaconis
plot_histogram(data = tbl, x = x)
# Use bins to manually choose number of bins
plot_histogram(data = tbl, x = x, bins = 50)
# Example of alternative methods: square root and Rice
plot_histogram(data = tbl, x = x, method = "sqrt")
plot_histogram(data = tbl, x = x, method = "Rice")

# To compare multiple groups use facet
tbl <- data.frame(
city = rep(c("A", "B", "C"), each.out = 500),
x = c(rnorm(500), runif(500), rexp(500))
)
plot_histogram(data = tbl, x = x, facet = city, density = TRUE)
}
