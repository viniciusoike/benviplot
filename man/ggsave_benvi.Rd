% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-plot.R
\name{ggsave_benvi}
\alias{ggsave_benvi}
\title{Save a ggplot with benviplot optimizations}
\usage{
ggsave_benvi(
  filename,
  plot = ggplot2::last_plot(),
  device = NULL,
  width = 7,
  height = 5,
  units = "in",
  dpi = 300,
  ...
)
}
\arguments{
\item{filename}{File name to create on disk. The file extension determines
the graphics device (e.g., ".png", ".pdf", ".svg").}

\item{plot}{Plot to save. Defaults to the last plot displayed.}

\item{device}{Device to use. Defaults to "ragg" for PNG files if ragg package
is installed, otherwise uses ggplot2's default device selection.}

\item{width, height}{Plot size in units. Defaults to 7 x 5 inches.}

\item{units}{Units for width and height ("in", "cm", "mm", "px"). Default is "in".}

\item{dpi}{DPI to use for raster graphics. Default is 300 for high quality.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly returns the filename.
}
\description{
A wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with smart defaults optimized for
benviplot graphics. Automatically uses the ragg graphics device for PNG
output when available, ensuring high-quality rendering with proper font
support and no DPI issues.

If ragg is not installed, falls back to the default graphics device.
}
\examples{
\dontrun{
library(ggplot2)

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_benvi()

# Save with automatic ragg device (if installed)
ggsave_benvi("my_plot.png", p)

# Save with custom dimensions
ggsave_benvi("my_plot.png", p, width = 10, height = 6)

# Save as PDF (uses default PDF device)
ggsave_benvi("my_plot.pdf", p)
}

}
\seealso{
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}
}
