# A base custom theme for ggplot plots

A base theme for ggplot plots in a Benvi style. Called by `theme_benvi`.

## Usage

``` r
theme_custom(...)
```

## Arguments

- ...:

  Further arguments to `theme_minimal`
