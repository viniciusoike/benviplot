# Add axis to a plot

Helper function to add axis to a plot.

## Usage

``` r
plot_add_xy(plot, type = "both")
```

## Arguments

- plot:

  A `ggplot` object

- type:

  One of `"none"`, `"x"`, `"y"`, or `"both"` (default)
